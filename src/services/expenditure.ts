import { IExpenditure } from "../interfaces/expenditure-interface"

const insertExpenditure = async (expenditure: IExpenditure): Promise<boolean> => {
    
    return true
}

export default { insertExpenditure }